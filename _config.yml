name: Powershop
exclude: [README.md, .gitignore, Gemfile, Gemfile.lock, Capfile, config]
permalinks: pretty
plugins: ./_plugins

assets:
  js_compressor: 'uglifier'
  css_compressor: 'yui'
  cache: false
